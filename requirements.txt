# Core libraries (you install torch separately via install script)
transformers==4.49.0
accelerate==1.5.2
bitsandbytes==0.42.0

# Optional: tokenizer/model loading helpers
sentencepiece>=0.1.99
safetensors>=0.4.3

# Dataset handling (optional but useful)
datasets>=2.17.0
huggingface-hub>=0.22.0

# Metrics/logging (optional)
tqdm
scikit-learn
pandas

# FlashAttention is installed separately due to its C++/CUDA build
# flash-attn==2.4.2  <-- DO NOT include this here
